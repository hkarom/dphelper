package experiments.decorator.decoratorWithoutAgregation;

import dpHelper.annotations.Decorator;

@Decorator(Decorator.Role.COMPONENT_CONCRETE)
public class ComponentConcret implements Component{
    public void methode(){

    }
}